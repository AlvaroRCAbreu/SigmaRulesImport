trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo "$(sigmaRulePath)"
  displayName: "Regra Informada"
# Etapa 1: Clonar repositório Git
- checkout: none
- script: |
    git clone https://github.com/SigmaHQ/sigma.git
  displayName: 'Clonar repositório Sigma do Git'

# Etapa 2: Instalar Sigma-CLI
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.x'
    addToPath: true

- script: |
    pip install sigma-cli  # Instala o Sigma-CLI
  displayName: 'Instalar Sigma-CLI'

# Etapa 3: Adicionar plugin ao Sigma-CLI
- script: |
    sigma plugin install datadog -f
  displayName: 'Adicionar Plugin do Datadog ao Sigma-CLI'

# Etapa 4: Rodar Sigma-CLI com a regra
- script: |
    sigma convert -t datadog "$(Build.SourcesDirectory)/sigma/$(sigmaRulePath)" -f siem_rule
  displayName: 'Executar Sigma-CLI com a regra'

# Etapa 5: Enviar resultado para o Datadog via API
- script: |
    SIGMA_OUTPUT=$(sigma convert -t datadog "$(Build.SourcesDirectory)/sigma/$(sigmaRulePath)" -f siem_rule)  # Captura a saída do comando sigma
    curl -X POST "https://api.datadoghq.com/api/v2/security_monitoring/rules" \
      -H "Content-Type: application/json" \
      -H "DD-API-KEY: %datadog_api_key%" \
      -H "DD-APPLICATION-KEY: %DD_APP_KEY%" \
      -d '{
            "title": "Sigma CLI Output",
            "text": "'"$SIGMA_OUTPUT"'",
            "priority": "normal",
            "tags": ["sigma"],
            "alert_type": "info"
          }'
  displayName: 'Enviar saída do Sigma-CLI para o Datadog'

